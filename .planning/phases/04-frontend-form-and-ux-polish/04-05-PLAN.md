---
phase: 04-frontend-form-and-ux-polish
plan: "05"
type: execute
wave: 3
depends_on:
  - "04-03"
files_modified:
  - CONTRIBUTING.md
  - README.md
autonomous: true
requirements:
  - DEV-04
  - DEV-05

must_haves:
  truths:
    - "CONTRIBUTING.md exists and covers local setup (docker compose up), running tests (composer run phpunit), running PHPCS (composer run phpcs), coding conventions, and PR submission"
    - "README.md exists and covers installation, settings configuration (all four tabs), and shortcode usage ([membership_form] and [membership_form download='blank'])"
    - "Both files are in the repository root"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Developer guide"
      contains: "docker compose"
    - path: "README.md"
      provides: "User-facing installation and usage guide"
      contains: "membership_form"
  key_links:
    - from: "README.md"
      to: "shortcode [membership_form]"
      via: "Usage section"
      pattern: "\\[membership_form\\]"
    - from: "CONTRIBUTING.md"
      to: "composer run phpunit"
      via: "Testing section"
      pattern: "composer run phpunit"
---

<objective>
Write CONTRIBUTING.md (developer guide) and README.md (user guide). These are the final artifacts to complete v1. They document the exact commands and configuration steps a developer or club admin needs — no speculation, all based on the actual implemented stack.

Purpose: Completes DEV-04 (CONTRIBUTING.md) and DEV-05 (README.md). Makes the plugin usable without tribal knowledge.
Output: CONTRIBUTING.md and README.md in project root.
</objective>

<execution_context>
@/home/jean-luc/.claude/get-shit-done/workflows/execute-plan.md
@/home/jean-luc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-form-and-ux-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - composer.json (exact script names: phpcs, phpunit, prefix-namespaces, post-install-cmd)
    - docker-compose.yml (exact service names, ports, WP URL)
    - .phpcs.xml (coding standards rules for accurate description)
    - wp-membership-registration.php (plugin version, PHP/WP requirements from plugin header)
    - .planning/STATE.md (decisions section: key patterns like PSR-4 autoloading, mu-plugin SMTP approach)
  </read_first>
  <action>
Create CONTRIBUTING.md in the project root. Content must reflect the actual implemented stack — read all listed files first to get exact command names, port numbers, and patterns.

Structure:

```markdown
# Contributing to WP Membership Registration Plugin

## Requirements

- Docker and Docker Compose
- PHP 8.0+ (for running Composer/tools outside Docker)
- Composer

## Local Development

### Start the environment

```bash
docker compose up -d
```

WordPress is available at http://localhost:8080.
Mailpit (email testing UI) is available at http://localhost:8025.
The plugin is mounted directly into the container — file changes are reflected immediately without rebuilding.

### First-time setup

```bash
composer install
```

Composer's `post-install-cmd` automatically runs Strauss to namespace-prefix vendor libraries into `vendor-prefixed/`. If you add a new production dependency, re-run:

```bash
composer run prefix-namespaces
```

### Activate the plugin

In the WordPress admin (http://localhost:8080/wp-admin), go to Plugins and activate **WP Membership Registration**. Or via WP-CLI inside the container:

```bash
docker compose exec wordpress wp plugin activate wp-membership-registration --allow-root
```

## Running Tests

Unit tests use PHPUnit with Brain Monkey (no WordPress bootstrap required):

```bash
composer run phpunit
```

Tests live in `tests/Unit/`. New test classes must follow the Brain Monkey setUp/tearDown pattern established in `tests/Unit/Email/MailerTest.php`. The `tests/bootstrap.php` file registers the PSR-4 autoloader and global i18n stubs.

## Coding Standards

```bash
composer run phpcs
```

The plugin enforces WordPress Coding Standards (WPCS) via PHPCS. The `.phpcs.xml` config:
- Targets `src/` and `wp-membership-registration.php`
- Excludes PSR-4 filename sniffs (`WordPress.Files.FileName.*`) to allow `ClassName.php` files
- Requires proper escaping, sanitization, and nonce verification on all user input

All pull requests must pass `composer run phpcs` with zero errors.

## Project Structure

```
src/
├── Admin/          # Settings page, registrar, AJAX handlers
├── Email/          # Mailer class
├── Frontend/       # FormRenderer (HTML form rendering)
├── Pdf/            # PdfGenerator (TCPDF-based)
├── Shortcodes/     # MembershipFormShortcode
└── Util/           # FieldSchema helper

assets/
├── css/            # admin-settings.css, form.css
└── js/             # admin-settings.js, form.js

templates/
└── admin-settings-page.php

tests/
└── Unit/
    ├── Admin/
    ├── Email/
    ├── Frontend/
    └── Pdf/

vendor/             # Composer dependencies (not committed)
vendor-prefixed/    # Strauss-prefixed vendor files (not committed — generated by prefix-namespaces)
```

## Vendor Namespacing

Production dependencies (currently TCPDF) are namespace-prefixed by Strauss to avoid conflicts with other plugins. The Strauss configuration is in `composer.json` under `extra.strauss`. The `vendor-prefixed/` directory is generated and excluded from git. After `composer install`, Strauss runs automatically via `post-install-cmd`.

## Pull Request Checklist

- [ ] `composer run phpcs` passes with zero errors
- [ ] `composer run phpunit` passes with zero failures
- [ ] New PHP classes follow PSR-4 naming in `src/` under `WpMembershipRegistration\` namespace
- [ ] All user input is sanitized; all output is escaped (WPCS rules enforced)
- [ ] New AJAX handlers call `check_ajax_referer()` as the first line
- [ ] No files written to `wp-content/uploads/` — PDFs use `sys_get_temp_dir()` + `wp_delete_file()`
- [ ] New production dependencies added to `extra.strauss.packages` if they need prefixing
```

Write this content verbatim, adjusting any port numbers or service names found in docker-compose.yml if they differ from 8080/8025.
  </action>
  <verify>
    <automated>test -f /home/jean-luc/git/bitstrange/wp-membership-registration-plugin/CONTRIBUTING.md && grep "docker compose" /home/jean-luc/git/bitstrange/wp-membership-registration-plugin/CONTRIBUTING.md && grep "composer run phpunit" /home/jean-luc/git/bitstrange/wp-membership-registration-plugin/CONTRIBUTING.md && grep "composer run phpcs" /home/jean-luc/git/bitstrange/wp-membership-registration-plugin/CONTRIBUTING.md && grep "check_ajax_referer" /home/jean-luc/git/bitstrange/wp-membership-registration-plugin/CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f CONTRIBUTING.md` exits 0
    - `grep "docker compose" CONTRIBUTING.md` returns a match (local setup command)
    - `grep "composer run phpunit" CONTRIBUTING.md` returns a match
    - `grep "composer run phpcs" CONTRIBUTING.md` returns a match
    - `grep "vendor-prefixed" CONTRIBUTING.md` returns a match (Strauss explanation)
    - `grep "check_ajax_referer" CONTRIBUTING.md` returns a match (PR checklist)
    - `grep "sys_get_temp_dir" CONTRIBUTING.md` returns a match (PDF file handling rule)
    - File length is >= 50 lines
  </acceptance_criteria>
  <done>CONTRIBUTING.md exists in project root. Covers: local setup, first-time install, running tests, coding standards, project structure, vendor namespacing, and PR checklist. All commands match the actual composer.json scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Write README.md</name>
  <files>README.md</files>
  <read_first>
    - wp-membership-registration.php (plugin name, version, description, PHP/WP requirements from plugin header comments)
    - src/Admin/SettingsRegistrar.php (option keys and field names for accurate settings documentation)
    - src/Shortcodes/MembershipFormShortcode.php (shortcode name and supported attributes)
    - .planning/REQUIREMENTS.md (v1 feature list for accurate capability description)
    - .planning/phases/04-frontend-form-and-ux-polish/04-CONTEXT.md (four tabs: Form Fields, PDF Branding, Email Settings, Form Settings)
  </read_first>
  <action>
Create README.md in the project root. Target audience: club admin (non-developer) who installs the plugin via WordPress admin. Content must be accurate — no invented commands.

Structure:

```markdown
# WP Membership Registration Plugin

A WordPress plugin for digital membership registration. Visitors fill in a form on your website, receive a pre-filled PDF membership form by email, and the club admin gets a copy. No paid software, no spreadsheets.

## Requirements

- WordPress 6.0 or later
- PHP 8.0 or later
- An SMTP plugin or hosting SMTP configuration for email delivery

## Installation

1. Download the plugin zip from the [Releases page](../../releases).
2. In your WordPress admin, go to **Plugins → Add New → Upload Plugin**.
3. Upload the zip and click **Activate**.

Or place the plugin folder in `wp-content/plugins/wp-membership-registration/` and activate it from the Plugins list.

## Configuration

Go to **Settings → Membership Registration** in your WordPress admin. The settings page has four tabs:

### Form Fields

Define the registration form fields that visitors fill in. Each field has:
- **Label** — the field name shown on the form and in the PDF
- **Type** — text, email, date, phone, textarea, or select
- **Required** — whether the field must be filled before submission

Use the **Add field**, **Remove**, and drag handles to manage fields. Click **Save** when done.

The first field with type **email** is used as the member's email address for the confirmation email.

### PDF Branding

Customize how the generated PDF looks:
- **Club name** — shown as the heading at the top of the PDF
- **Document title** — subtitle below the club name (e.g. "Membership Application")
- **Accent color** — hex color used for headings
- **Club logo** — displayed in the PDF header (upload via Media Library)
- **Form information** — rich text shown below the form fields on both the PDF and the registration form. Use this for GDPR notice, membership conditions, or any explanatory text.
- **Footer text** — small text printed at the bottom of page 1
- **Page 2 content** — rich text printed on page 2 of the PDF (use for AGBs, Datenschutzerklärung, bank details)

### Email Settings

Enter the admin email addresses that receive a copy of each PDF after submission — one address per line. Click **Send test email** to verify your SMTP connection is working before going live.

### Form Settings

- **Consent checkbox text** — the label shown next to the GDPR consent checkbox. Submission is blocked unless this is checked.
- **Success message** — the message shown in-place after a successful form submission.

## Shortcodes

### Registration form

```
[membership_form]
```

Embed the registration form on any page or post. Add the shortcode to a page — the form is rendered automatically from your Form Fields settings.

### Blank PDF download

```
[membership_form download="blank"]
```

Renders a download link for a blank (empty) version of the membership form PDF. Members who prefer to fill in a paper form can download and print this version. The blank PDF contains interactive fillable fields.

## How It Works

1. A visitor fills in and submits the registration form.
2. The plugin generates a pre-filled PDF with the submitted data.
3. The visitor receives the PDF by email as confirmation.
4. All configured admin recipients also receive a copy of the PDF.
5. The PDF is deleted from the server immediately after sending.

## Privacy

- No form submission data is stored in the database — the plugin is stateless.
- The generated PDF exists only in the server's temp directory for the seconds it takes to email it, then it is deleted.
- You are responsible for ensuring your privacy policy covers the data processing described above.

## License

GPL-2.0-or-later. See [LICENSE](LICENSE).
```

Adjust the plugin name and admin menu label to match what is in wp-membership-registration.php if they differ.
  </action>
  <verify>
    <automated>test -f /home/jean-luc/git/bitstrange/wp-membership-registration-plugin/README.md && grep "membership_form" /home/jean-luc/git/bitstrange/wp-membership-registration-plugin/README.md && grep "Installation" /home/jean-luc/git/bitstrange/wp-membership-registration-plugin/README.md && grep "Form Settings" /home/jean-luc/git/bitstrange/wp-membership-registration-plugin/README.md && grep "PDF Branding" /home/jean-luc/git/bitstrange/wp-membership-registration-plugin/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` exits 0
    - `grep "\[membership_form\]" README.md` returns a match (shortcode usage)
    - `grep "download=\"blank\"" README.md` returns a match (blank PDF shortcode)
    - `grep "## Installation" README.md` returns a match
    - `grep "Form Fields" README.md` returns a match (settings tab)
    - `grep "PDF Branding" README.md` returns a match (settings tab)
    - `grep "Email Settings" README.md` returns a match (settings tab)
    - `grep "Form Settings" README.md` returns a match (fourth settings tab)
    - `grep "consent" README.md` returns a match (GDPR explanation)
    - File length is >= 60 lines
  </acceptance_criteria>
  <done>README.md exists in project root. Covers: requirements, installation, all four settings tabs, both shortcodes, how the pipeline works, and privacy note. Accurate to the implemented plugin.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `test -f CONTRIBUTING.md` exits 0
2. `test -f README.md` exits 0
3. `grep "membership_form" README.md` returns a match
4. `grep "composer run phpunit" CONTRIBUTING.md` returns a match
</verification>

<success_criteria>
- CONTRIBUTING.md covers: docker compose setup, composer install, phpunit, phpcs, project structure, Strauss, PR checklist
- README.md covers: installation, all four settings tabs with field descriptions, both shortcode variants, privacy note
- Both files exist in project root
- No invented commands — all match actual composer.json scripts and project structure
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-form-and-ux-polish/04-05-SUMMARY.md`
</output>
