---
phase: 05-php-i18n-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - languages/.gitkeep
  - src/Plugin.php
  - tests/Unit/PluginTest.php
autonomous: true
requirements:
  - I18N-03

must_haves:
  truths:
    - "load_plugin_textdomain() is called on the init hook with correct text domain and languages/ path"
    - "languages/ directory exists in the plugin root"
    - "Text domain 'wp-membership-registration' matches the plugin header"
  artifacts:
    - path: "languages/.gitkeep"
      provides: "Languages directory for .mo files"
    - path: "src/Plugin.php"
      provides: "Text domain registration on init"
      contains: "load_plugin_textdomain"
    - path: "tests/Unit/PluginTest.php"
      provides: "Unit tests for text domain registration"
  key_links:
    - from: "src/Plugin.php"
      to: "languages/"
      via: "load_plugin_textdomain third argument"
      pattern: "dirname.*plugin_basename.*languages"
---

<objective>
Register the plugin text domain on the `init` hook and create the `languages/` directory.

Purpose: I18N-03 requires `load_plugin_textdomain()` on init pointing at the plugin's `languages/` directory. This is the foundation all other i18n work depends on -- without it, `.mo` files (Phase 6) will never load.
Output: `languages/.gitkeep`, updated `src/Plugin.php`, new `tests/Unit/PluginTest.php`
</objective>

<execution_context>
@/home/jean-luc/.claude/get-shit-done/workflows/execute-plan.md
@/home/jean-luc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-php-i18n-foundation/05-RESEARCH.md
@.planning/phases/05-php-i18n-foundation/05-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From wp-membership-registration.php (lines 11-12):
```php
 * Text Domain: wp-membership-registration
 * Domain Path: /languages
```

From src/Plugin.php — current register() method signature:
```php
namespace WpMembershipRegistration;

class Plugin {
    public function register(): void {
        // ... existing hook registrations ...
        // wp_enqueue_scripts closure at end (lines 51-90) — localize_script for wmrForm
    }
}
```

From tests/bootstrap.php — Brain Monkey already configured, stubs for __(), esc_html_e() etc. available.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PluginTest.php and languages/ directory</name>
  <files>tests/Unit/PluginTest.php, languages/.gitkeep</files>
  <behavior>
    - Test 1: Plugin::register() adds an action on 'init' hook
    - Test 2: The init callback calls load_plugin_textdomain() with 'wp-membership-registration' as text domain
    - Test 3: The init callback passes the correct relative path ending in '/languages' as third argument
  </behavior>
  <action>
1. Create `languages/.gitkeep` (empty file) so the directory exists in the repo.

2. Create `tests/Unit/PluginTest.php` with three tests covering I18N-03:
   - `test_register_hooks_init_action`: Assert that `Plugin::register()` calls `add_action('init', ...)`. Use Brain Monkey's `Actions\expectAdded('init')`.
   - `test_init_calls_load_plugin_textdomain`: Mock the init callback, invoke it, and assert `load_plugin_textdomain` was called with `'wp-membership-registration'` as first arg and `false` as second arg.
   - `test_init_textdomain_path_points_to_languages_dir`: Assert the third argument to `load_plugin_textdomain` ends with `'/languages'`.

Brain Monkey test patterns: use `Functions\expect('load_plugin_textdomain')->once()->with(...)` to verify the call. Define `WMR_PLUGIN_FILE` constant in the test if not already defined.

The test setUp must stub all WP functions that Plugin::register() calls (add_action, add_shortcode, get_option, wp_kses_post, wp_enqueue_style, wp_enqueue_script, wp_localize_script, wp_create_nonce, is_singular, is_a, has_shortcode, etc.) -- follow the existing pattern in FormRendererTest.php for Brain Monkey stubs.

Run RED: tests should fail because Plugin.php does not yet call load_plugin_textdomain.
  </action>
  <verify>
    <automated>cd /home/jean-luc/git/bitstrange/wp-membership-registration-plugin && ./vendor/bin/phpunit --filter PluginTest 2>&1 | tail -5</automated>
  </verify>
  <done>tests/Unit/PluginTest.php exists with 3 test methods; languages/.gitkeep exists; tests fail (RED) because Plugin.php lacks load_plugin_textdomain</done>
</task>

<task type="auto">
  <name>Task 2: Add load_plugin_textdomain() to Plugin::register()</name>
  <files>src/Plugin.php</files>
  <action>
Add an `add_action('init', ...)` call at the TOP of `Plugin::register()` (before other hook registrations). The init callback must:

1. Call `load_plugin_textdomain('wp-membership-registration', false, dirname(plugin_basename(WMR_PLUGIN_FILE)) . '/languages')`.

That is all for this plan. The `switch_to_locale()` logic (I18N-07) is added in plan 05-03.

Use a static closure for the callback (consistent with the existing wp_enqueue_scripts closure pattern in Plugin.php).

Do NOT add switch_to_locale() yet -- that belongs to plan 05-03 which handles the fallback language setting.
  </action>
  <verify>
    <automated>cd /home/jean-luc/git/bitstrange/wp-membership-registration-plugin && ./vendor/bin/phpunit --filter PluginTest 2>&1 | tail -5</automated>
  </verify>
  <done>All PluginTest tests pass (GREEN). load_plugin_textdomain() is hooked on init with correct text domain and path. Full suite still green: ./vendor/bin/phpunit</done>
</task>

</tasks>

<verification>
- `./vendor/bin/phpunit --filter PluginTest` -- all 3 tests green
- `./vendor/bin/phpunit` -- full suite green (no regressions)
- `languages/.gitkeep` exists
- `grep -c 'load_plugin_textdomain' src/Plugin.php` returns 1
</verification>

<success_criteria>
- load_plugin_textdomain() called on init with 'wp-membership-registration' domain and '/languages' path
- languages/ directory exists with .gitkeep
- PluginTest.php covers the registration with 3 passing tests
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/05-php-i18n-foundation/05-01-SUMMARY.md`
</output>
