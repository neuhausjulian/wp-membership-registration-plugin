---
phase: 03-email-notifications
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
  - 03-03
files_modified: []
autonomous: false
requirements:
  - CONF-05
must_haves:
  truths:
    - "Clicking 'Send Test Email' in Email Settings tab triggers a visible success response"
    - "The test email appears in Mailpit at localhost:8025 within seconds"
    - "The Email Settings tab renders without PHP errors and shows the warning notice and email preview boxes"
    - "Full PHPUnit test suite passes before human verification"
  artifacts: []
  key_links:
    - from: "WordPress admin Email Settings tab"
      to: "Mailpit inbox (localhost:8025)"
      via: "wp_ajax_wmr_send_test_email → wp_mail() → PHPMailer → SMTP → Mailpit"
      pattern: "wmr_send_test_email"
---

<objective>
Verify the complete Phase 3 delivery end-to-end using the live Docker + Mailpit environment. This checkpoint confirms CONF-05 (test email button) works through the full SMTP stack, and confirms the new Email Settings tab UI additions render correctly.

Purpose: PHPUnit tests stub `wp_mail()` — only a live Docker run catches SMTP misconfigurations, PHPMailer errors, and rendering issues in the browser. This is the Phase 3 gate before marking the phase complete.

Output: Human confirmation that test email arrives in Mailpit and the settings tab UI is correct.
</objective>

<execution_context>
@/home/jean-luc/.claude/get-shit-done/workflows/execute-plan.md
@/home/jean-luc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-email-notifications/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm green before checkpoint</name>
  <files>none</files>

  <read_first>
    - .planning/phases/03-email-notifications/03-VALIDATION.md (sampling rate and full suite command)
  </read_first>

  <action>
    Run the full PHPUnit test suite. All tests must pass before proceeding to the human verification checkpoint.

    ```bash
    vendor/bin/phpunit
    ```

    If any test fails, stop and fix before continuing. Do not proceed to the human checkpoint with a failing test suite.

    Also run PHPCS on all modified files:
    ```bash
    vendor/bin/phpcs src/Email/Mailer.php src/Plugin.php templates/admin-settings-page.php
    ```
  </action>

  <verify>
    <automated>vendor/bin/phpunit 2>&1 | tail -3</automated>
  </verify>

  <done>
    - `vendor/bin/phpunit` exits 0
    - `vendor/bin/phpcs src/Email/Mailer.php src/Plugin.php templates/admin-settings-page.php` exits 0
    - Output shows "OK (N tests, N assertions)" with no failures or errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end CONF-05 and UI verification in Docker + Mailpit</name>
  <files>none</files>

  <action>
    Human verification step — no automated action. Follow the how-to-verify instructions below.
  </action>

  <verify>
    <automated>MANUAL — see how-to-verify steps below. Automated verification not possible for live SMTP + browser UI.</automated>
  </verify>

  <done>
    - Test email appears in Mailpit at http://localhost:8025 with subject "[Test] Membership Registration email"
    - Email Settings tab renders without PHP errors
    - Email Previews section visible with correct subject/body for both member and admin emails
    - Inline warning appears when no email-type field configured
    - Human types "approved" in resume-signal
  </done>

  <what-built>
    Phase 3 complete: Mailer class (src/Email/Mailer.php), hook wiring (Plugin.php), Email Settings tab UI extension (templates/admin-settings-page.php). Test email button (CONF-05) was implemented in Phase 1 and routes through the full SMTP stack.
  </what-built>

  <how-to-verify>
    **Step 1 — Start the Docker environment (if not already running):**
    ```bash
    docker compose up -d
    ```
    Wait ~10 seconds for WordPress to initialize.

    **Step 2 — Confirm Mailpit is accessible:**
    Open http://localhost:8025 in your browser. You should see the Mailpit inbox (empty or with prior test emails).

    **Step 3 — Open WordPress admin Email Settings tab:**
    Navigate to http://localhost:8080/wp-admin/
    Log in with WordPress admin credentials (admin / password, or as configured in docker-compose.yml).
    Go to Settings > Membership Registration.
    Click the "Email Settings" tab.

    **Step 4 — Verify the UI additions:**
    - [ ] "Email Previews" section is visible below the Save Settings button, showing two rows: "Member email" and "Admin email"
    - [ ] Member email row shows subject "Your registration form" and body text about the attachment
    - [ ] Admin email row shows subject "New membership registration received"
    - [ ] If no email-type field is configured: an orange/yellow inline warning appears above the form saying "No email-type field is configured in the form — members will not receive a confirmation email."
    - [ ] If an email-type field IS configured: no warning visible

    **Step 5 — Ensure at least one recipient email is configured:**
    In the "Notification Recipients" textarea, enter: test@example.com
    Click "Save Settings".

    **Step 6 — Click "Send Test Email":**
    Click the "Send Test Email" button.
    Expected: A green success message appears inline: "Test email sent."

    **Step 7 — Confirm email in Mailpit:**
    Switch to Mailpit at http://localhost:8025
    Expected: A new email appears in the inbox with subject "[Test] Membership Registration email"
    Open the email and confirm the body text: "This is a test email from the WP Membership Registration plugin."

    **If step 6 shows an error instead of success:**
    Check http://localhost:8025 anyway — the email may still have arrived.
    Check WordPress debug log: `docker compose logs wordpress 2>&1 | grep wmr`
  </how-to-verify>

  <resume-signal>
    Type "approved" if all 7 steps passed. Or describe which step failed and what you observed.
  </resume-signal>
</task>

</tasks>

<verification>
- `vendor/bin/phpunit` — all tests pass (automated, Task 1)
- CONF-05 test email visible in Mailpit at localhost:8025 (manual, Task 2)
- Email Settings tab shows Email Previews section (manual, Task 2)
- Inline warning appears/disappears correctly based on schema (manual, Task 2)
</verification>

<success_criteria>
Full PHPUnit suite is green. A test email sent via the WordPress admin settings page appears in Mailpit within seconds. The Email Settings tab renders the warning notice and email preview boxes correctly. Phase 3 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-email-notifications/03-04-SUMMARY.md`
</output>
